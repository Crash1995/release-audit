# Install release-audit for Claude Code

Global install:

```bash
mkdir -p ~/.claude/skills
git clone https://github.com/Crash1995/release-audit.git ~/.claude/skills/release-audit
```

Project install:

```bash
mkdir -p .claude/skills
git clone https://github.com/Crash1995/release-audit.git .claude/skills/release-audit
```

Verify:

```text
Run a full release audit for this repository.
```
