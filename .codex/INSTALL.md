# Install release-audit for Codex

```bash
mkdir -p ~/.codex/skills
git clone https://github.com/Crash1995/release-audit.git ~/.codex/skills/release-audit
```

Verify:

```text
Use $release-audit and run a full release audit for this repository.
```
