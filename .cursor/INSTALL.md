# Install release-audit for Cursor

Global install:

```bash
mkdir -p ~/.cursor/skills
git clone https://github.com/Crash1995/release-audit.git ~/.cursor/skills/release-audit
```

Project install:

```bash
mkdir -p .cursor/skills
git clone https://github.com/Crash1995/release-audit.git .cursor/skills/release-audit
```

Verify:

```text
Run a full release audit for this repository.
```
